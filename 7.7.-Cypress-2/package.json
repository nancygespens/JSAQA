{
  "name": "7.7.-cypress-2",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "chrome": "npx cypress run --config-file cypress.config.js --browser chrome",
    "chrome_headed": "npx cypress run --config-file cypress.config.js --browser chrome --headed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^10.11.0"
  },
  "description": "Любые вопросы по решению задач задавайте в чате учебной группы."
}
